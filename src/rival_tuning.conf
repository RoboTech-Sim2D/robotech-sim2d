# Ajuste por rival — ver src/player/rival_tuning.h para formato y semántica.
# Sin entrada para un rival = defaults = equipo actual (idéntico).
#
# Perillas: pressing (base de persecución, default 25)
#           deep_press_x (x máx. de press profundo de defensas, default -20)
#           wing_x wing_y (trigger del wing-tactic, default 15 12)
#
# PROBADO Y DESCARTADO (tanda 2026-07-13, 10 partidos): "HELIOS pressing 13"
# → GA 8.1 (borde exacto de su banda 8.1-9.7), 0-0-10, sin efecto medible.
# La brecha vs HELIOS es estructural (tier), no del tamaño de una perilla.
# El mecanismo queda validado neutro: controles sin entrada (SRBIAU/RoboCIn)
# en rango histórico. Ejemplo de formato (comentado):
# HELIOS pressing 13
